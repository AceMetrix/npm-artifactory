var should = require('chai').should();
var express = require('express');
var npm = require('npm');

describe('publish', function(){
    /*
    before(function(done){
        npm.load({registry: 'http://localhost:3000'}, done);
    });
    it('publishes the module to artifactory', function(done){
    });
    */
});
